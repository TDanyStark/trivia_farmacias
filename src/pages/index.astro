---
import Header from "@/components/Header.astro";
import Layout from "../layouts/Layout.astro";
import Footer from "@/components/Footer.astro";
import { URL_BASE } from "@/config";
---

<Layout>
  <div
    class="min-h-screen flex flex-col bg-purple-abbott bg-cover bg-left lg:bg-center text-white "
    style={`background-image: url(${URL_BASE}/images/bg-home.webp);`}
  >
    <Header />
    <main
      class="flex-1 grid grid-cols-1 md:grid-cols-2 max-w-7xl mx-auto p-4 md:p-6 gap-4"
    >
      <section class="flex flex-col items-start gap-6 justify-center">
        <h1 class="text-5xl lg:text-6xl font-light leading-[1.1] text-center lg:text-left">
          EL <span class="font-bold">PODER</span> QUE DA
          EL <span class="font-bold">CONOCIMIENTO</span>
        </h1>
        <div class="uppercase text-2xl text-center lg:text-left">
          <h2 class="font-bold">¡Ponga a prueba su conocimiento</h2>
          <p>
            Sobre las propiedades diferenciales de las diferentes combinaciones
            hormonales presentes en los métodos anticonceptivos!
          </p>
        </div>
				<a href={`${URL_BASE}/trivia`} class="mt-4 inline-block bg-pink-abbott text-white font-bold py-3 px-6 rounded-lg text-2xl hover:bg-purple-abbott transition text-center">
					HAZ CLIC AQUÍ PARA EMPEZAR
				</a>
      </section>
      <section>
        <img
          src={`${URL_BASE}/images/womans-home.webp`}
          alt="Mujeres Abbott"
          class="w-full h-auto lg:hidden"
        />
      </section>
    </main>
		<Footer />
  </div>
</Layout>
